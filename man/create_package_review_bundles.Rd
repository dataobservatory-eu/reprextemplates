% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_package_review_bundles.R
\name{create_package_review_bundles}
\alias{create_package_review_bundles}
\title{Create review bundles for R packages}
\usage{
create_package_review_bundles(root_dir = here::here())
}
\arguments{
\item{root_dir}{Root directory to search recursively for
R packages. Defaults to \code{\link[here:here]{here::here()}}.}
}
\value{
Invisibly returns a character vector containing the
package directories that were processed.
}
\description{
Recursively scan a directory tree for R packages and create
package review bundles using \code{\link[=concat_ordered_text_files]{concat_ordered_text_files()}}.
}
\details{
A directory is considered an R package if it contains both
\code{DESCRIPTION} and \code{NAMESPACE}.

For each package found, the function:
\itemize{
\item Creates a review bundle named
\verb{package_review-[package_name].txt}
\item Uses the \code{"R package"} profile from
\code{\link[=concat_files_profile]{concat_files_profile()}}
\item Adds the review bundle filename to \code{.gitignore}
\item Adds the review bundle filename pattern to \code{.Rbuildignore}
}

Existing ignore entries are not duplicated.
}
\examples{
\dontrun{

create_package_review_bundles(
  root_dir = "D:/_packages"
)

create_package_review_bundles(
  root_dir = here::here("..")
)
}

}
