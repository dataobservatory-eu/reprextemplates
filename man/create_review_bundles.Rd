% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_review_bundles.R
\name{create_review_bundles}
\alias{create_review_bundles}
\title{Create review bundles for supported project types}
\usage{
create_review_bundles(
  root_dir = NULL,
  exclude_patterns = c("/tests/testthat/testdata/", "/inst/")
)
}
\arguments{
\item{root_dir}{Root directory to search recursively.
Defaults to \code{\link[here:here]{here::here()}} when \code{NULL}.}

\item{exclude_patterns}{Defaults to \code{c("/tests/testthat/testdata/", "/inst/extdata/")}}
}
\value{
Invisibly returns a character vector containing the
directories for which review bundles were created.
}
\description{
Recursively scans a directory tree, detects supported project types,
and creates review bundles using the corresponding profile from
\code{\link[=concat_files_profile]{concat_files_profile()}}.
}
\details{
Currently supported project types are:
\itemize{
\item \code{r_package}: directories containing both \code{DESCRIPTION} and \code{NAMESPACE}
\item \code{quarto}: directories containing \verb{_quarto.yml}
\item \code{hugo}: directories containing \code{config.yaml}, \code{config.yml},
or \code{config.toml}
}

For each detected project, the function:
\itemize{
\item Creates a review bundle named \verb{review-[project_name].txt}
\item Uses the matching profile from \code{\link[=concat_files_profile]{concat_files_profile()}}
\item Adds the review bundle to \code{.gitignore}
\item Adds the review bundle to \code{.Rbuildignore} for R packages
}

Directories that do not match a known project profile are ignored.
}
\examples{
\dontrun{

create_review_bundles()

create_review_bundles(
  root_dir = "D:/allcontent"
)

create_review_bundles(
  root_dir = here::here("..")
)
}

}
\seealso{
\code{\link[=concat_files_profile]{concat_files_profile()}},
\code{\link[=concat_ordered_text_files]{concat_ordered_text_files()}}
}
