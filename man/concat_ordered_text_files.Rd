% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat_ordered_text_files.R
\name{concat_ordered_text_files}
\alias{concat_ordered_text_files}
\title{Concatenate ordered text files into a single file}
\usage{
concat_ordered_text_files(
  source_dir,
  groups,
  output_file = "combined.txt",
  profile = NULL,
  recursive = TRUE,
  add_source_comment = TRUE,
  comment_start = "<!---",
  comment_end = "--->",
  source_comment = c("relative_path", "filename", "absolute_path")
)
}
\arguments{
\item{source_dir}{Directory containing the source files.
For R package projects, \code{here::here()} is often a convenient way
to refer to the package root directory.}

\item{groups}{A list defining the concatenation order. Each element
should contain either:
\describe{
\item{files}{Character vector of explicit file names.}
\item{folder}{Directory name relative to \code{source_dir}.}
\item{extension}{Optional character vector of file extensions
used to filter files within a folder.}
}}

\item{output_file}{Path to the output file.}

\item{profile}{Optional name of the profile used to construct
\code{groups}. When supplied, the profile name is included in the
generated bundle header. See \code{\link[=concat_files_profile]{concat_files_profile()}} for
predefined profiles.}

\item{recursive}{Logical. Should folder searches recurse into
subdirectories?}

\item{add_source_comment}{Logical. Add source file comments before
each file.}

\item{comment_start}{Opening comment delimiter.}

\item{comment_end}{Closing comment delimiter.}

\item{source_comment}{One of \code{"relative_path"}, \code{"filename"},
or \code{"absolute_path"}.}
}
\value{
Invisibly returns the path to the created file.
}
\description{
Concatenate text files from explicitly ordered groups of files and
directories into a single output file. This function is useful for
creating review bundles, package summaries, LLM context files,
documentation archives, or forensic snapshots.
}
\details{
Files are processed in the order defined by \code{groups}. Within each
directory group, files are sorted alphabetically to ensure
reproducibility.
}
\examples{
example_dir <- tempfile("bundle")
dir.create(example_dir)

writeLines(
  "Package: examplepkg",
  file.path(example_dir, "DESCRIPTION")
)

writeLines(
  "# Example package",
  file.path(example_dir, "README.md")
)

output_file <- file.path(
  tempdir(),
  "package_review.txt"
)

groups <- list(
  list(
    files = c(
      "DESCRIPTION",
      "README.md"
    )
  )
)

concat_ordered_text_files(
  source_dir = example_dir,
  groups = groups,
  output_file = output_file
)

file.exists(output_file)
}
