% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media_table.R
\name{media_table}
\alias{media_table}
\title{Create a media and text layout for Quarto}
\usage{
media_table(x, widths = NULL, image_width = "100\%", valign = "middle")
}
\arguments{
\item{x}{A matrix containing \code{\link[=media]{media()}} objects and/or character values.
The matrix geometry determines the geometry of the resulting layout.}

\item{widths}{Optional numeric vector containing one relative width for
each column. Values are normalised, so \code{c(50, 50)}, \code{c(1, 1)} and
\code{c(0.5, 0.5)} are equivalent. If \code{NULL}, columns receive equal widths.}

\item{image_width}{Character scalar giving the default width of images
within their cells. Defaults to \code{"100\%"}. A width specified directly
in \code{\link[=media]{media()}} overrides this value.}

\item{valign}{Character scalar giving vertical alignment for HTML cells.
Defaults to \code{"middle"}. In LaTeX/PDF output, cells use vertically
centred \code{m} columns.}
}
\value{
A \code{knitr_kable} object appropriate to the current output format.
}
\description{
Creates a rectangular, headerless layout containing images and Markdown
text. The function is intended for visual placement rather than for
displaying tabular data.
}
\details{
Image cells are created with \code{\link[=media]{media()}}. All other cells are interpreted
as text.

For HTML output, text cells are passed to Quarto as Markdown using
embedded \code{data-qmd} content. For LaTeX/PDF output, Markdown is passed to
Quarto using \verb{\\\\QuartoMarkdownBase64\{\}}. This allows ordinary Markdown
formatting, such as emphasis and inline code, to be retained across
output formats.

The generated layout has no column headers. Captions, labels and
cross-references should be supplied by Quarto around the resulting
layout rather than by \code{media_table()} itself.

HTML and LaTeX/PDF output are rendered with \code{knitr} and \code{kableExtra}.
Other output formats, including DOCX, use a Markdown pipe table with
Pandoc image-width attributes.

The LaTeX implementation currently uses a total table width of 15 cm.
Column widths are allocated proportionally within that width.

Quarto citations are not supported inside text passed through
\verb{\\\\QuartoMarkdownBase64\{\}}. Ordinary Markdown formatting is supported.
}
\examples{
\dontrun{
media_table(
  matrix(
    list(
      media("images/farmhouse.jpg"),
      paste(
        "We created five images of the",
        "*farmhouse* from two directions."
      )
    ),
    nrow = 1
  ),
  widths = c(50, 50)
)

media_table(
  matrix(
    list(
      media("images/image1.jpg"),
      media("images/image2.jpg"),
      "Some *formatted* explanatory text.",
      media("images/image3.jpg")
    ),
    nrow = 2,
    byrow = TRUE
  ),
  widths = c(50, 50)
)
}

}
